import { Footer } from 'widgets/footer';

const PageNotFound = () => {
    return (
        <>
            <main>Not found</main>
            <Footer isAbsolute={true} />
        </>
    );
};

export default PageNotFound;
